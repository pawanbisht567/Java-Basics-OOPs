To Build the Code, below command will generate the .class file

```
    javac <file-name>.java
```

To run the code, this below file should be run without any extension name

```
    java <file-name>
```

Important Notes

```
    1. JVM is inside of JRE, JRE have all the utility libraries to use in our code.
    2. JDK -> JRE -> JVM
```

#Method Overloading
When we have multiple methods of same name but different signature, means diff parameter number, diff parameters type etc.

#stack and Heaps
3 hour:40 min (watch it)

Here’s the Java primitive data type size table you should memorize for interviews.

## Java Primitive Data Types

| Data Type |                  Size | Range / Values                  | Default Value |
| --------- | --------------------: | ------------------------------- | ------------- |
| `byte`    |       8 bits (1 byte) | -128 to 127                     | `0`           |
| `short`   |     16 bits (2 bytes) | -32,768 to 32,767               | `0`           |
| `int`     |     32 bits (4 bytes) | -2³¹ to 2³¹ - 1                 | `0`           |
| `long`    |     64 bits (8 bytes) | -2⁶³ to 2⁶³ - 1                 | `0L`          |
| `float`   |     32 bits (4 bytes) | ~6–7 decimal digits precision   | `0.0f`        |
| `double`  |     64 bits (8 bytes) | ~15–16 decimal digits precision | `0.0d`        |
| `char`    |     16 bits (2 bytes) | 0 to 65,535 (UTF-16 code unit)  | `'\u0000'`    |
| `boolean` | Not specified by Java | `true` / `false`                | `false`       |

#Array
Arrays are also an object because we create them using the 'new' Keyword
